{
  "name": "yerba-mate-home",
  "version": "1.0.0",
  "author": "Sobek",
  "scripts": {
    "dev": "concurrently 'react-scripts start' 'npm run watch -s'",
    "watch": "sass --watch ./src/styles/sass/index.scss ./src/styles/css/index.css",
    "build": "react-scripts build"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-router-dom": "^5.2.0",
    "react-scripts": "^4.0.3"
  },
  "devDependencies": {
    "chokidar": "^3.5.1",
    "concurrently": "^6.0.1"
  },
  "browserslist": {
    "production": [
      "defaults"
    ],
    "development": [
      "defaults"
    ]
  }
}
